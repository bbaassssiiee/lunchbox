    - name: 'fix JAVA_HOME'
      become: yes
      shell: echo export JAVA_HOME=/usr/lib/jvm/jre > /etc/profile.d/jre.sh

    - name: 'fix permissions'
      become: yes
      file: dest=/etc/profile.d/jre.sh mode=0755

#    - name: 'fix "web" in /etc/sysconfig/iptables file'
#      lineinfile: dest=/etc/sysconfig/iptables
#                  line='-A INPUT -p tcp -m tcp --dport 80 -j ACCEPT'
#                  state=present
#                  insertbefore=^COMMIT
#      notify: restart iptables

